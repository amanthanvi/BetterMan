{
  "name": "betterman-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev:backend & npm run dev:frontend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "cd backend && uvicorn src.main:app --reload",
    "build": "npm run build:frontend",
    "build:frontend": "npm run build --workspace=frontend",
    "start": "npm run start:backend & npm run start:frontend",
    "start:frontend": "npm run start --workspace=frontend",
    "start:backend": "cd backend && uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "lint": "npm run lint --workspace=frontend",
    "type-check": "npm run type-check --workspaces",
    "test": "npm run test:frontend && npm run test:backend",
    "test:frontend": "npm run test --workspace=frontend",
    "test:backend": "cd backend && python -m pytest",
    "clean": "rm -rf node_modules frontend/node_modules shared/node_modules frontend/.next backend/__pycache__",
    "install:all": "npm install && cd backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}