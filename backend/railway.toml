# This file is for the backend service
# The extractor service has its own directory at /backend/extractor
[build]
builder = "DOCKERFILE"
# Use the default Dockerfile for backend service

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3